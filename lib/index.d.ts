/**
 * dsh-prompt-optimizer — type declarations for the host entry.
 * The browser half is a module-loader bundle (lib/client.js) with no
 * exported types.
 */
export declare const name: string;
export declare const inject: string[];
export declare function apply(ctx: unknown): void;
